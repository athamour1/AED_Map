{
  "name": "aed-map",
  "version": "0.0.1",
  "private": true,
  "description": "AED Map monorepo — public locator PWA, admin panel, and API",
  "type": "module",
  "scripts": {
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev:public": "pnpm --filter aed-map-public dev"
  },
  "engines": {
    "node": "^28 || ^26 || ^24 || ^22.12",
    "pnpm": ">= 10.0.0"
  }
}
